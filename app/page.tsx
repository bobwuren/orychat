/**
 * app/page.tsx — Homepage publique (route "/")
 *
 * Header et Footer sont injectés automatiquement par ConditionalShell
 * dans app/layout.tsx — rien à faire ici.
 */
export { default } from "@/components/home/HomePage";
